fragment UserRepositories on RepositoryConnection {
    nodes {
        id
        nameWithOwner
        description
        url
        createdAt
        updatedAt
        stargazers(first: $firstCount) {
            totalCount
        }
    }
}

